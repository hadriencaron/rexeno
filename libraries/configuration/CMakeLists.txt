link_directories(${DIR3PARTY}/lib)
include_directories(${DIR3PARTY}/include)
if (STATIC)
  add_library(configuration parser.cc)
else()
  add_library(configuration SHARED parser.cc)
endif()
install_targets(/lib configuration)
