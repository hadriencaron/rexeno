#find_package (glut)
link_libraries (glut)
add_library (session session.cc trial.cc shape.cc square.cc)
target_link_libraries (configuration)
link_libraries(configuration)
add_executable (sessionBin session.cc trial.cc mainSession.cc shape.cc square.cc)
include_directories ("${PROJECT_SOURCE_DIR}/includes")
include_directories ("${PROJECT_SOURCE_DIR}/includes")
include_directories ("${PROJECT_SOURCE_DIR}/configuration")
