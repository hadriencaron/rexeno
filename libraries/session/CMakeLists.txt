#find_package (glut)
set(CMAKE_CXX_FLAGS_INIT "-Wall --warn-uninitialized --warn-unused-vars")
link_libraries (glut)
add_library (session session.cc trial.cc shape.cc square.cc rectangle.cc variable.cc setup.cc)
target_link_libraries (configuration)
link_libraries(configuration)
add_executable (sessionBin session.cc trial.cc mainSession.cc shape.cc square.cc rectangle.cc variable.cc setup.cc)
include_directories ("${PROJECT_SOURCE_DIR}/includes")
include_directories ("${PROJECT_SOURCE_DIR}/includes")
include_directories ("${PROJECT_SOURCE_DIR}/configuration")
